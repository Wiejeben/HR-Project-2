import pygame

class Text:
    def __init__(self, content, position, fontsize, color):
        self.content = content
        self.position = position
        self.fontsize = fontsize
        self.color = color

    def DrawText(self):
        return content + position + fontsize + color

print ("Before the game starts")
print ("At the start, every player gets $10.000")
print ("Every player gets an �own park�. This is the little board. Here you put every") 
print ("Every player gets an essential card. You need to complete the objective on the ")
print ("card in order to win the game.")